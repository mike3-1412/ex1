# ex1
git pull lấy dữ liệu từ trên kho repo
git fetch kiểm tra các lần thay đổi 
git checkout di chuyển và xem lại lịch sử
git log xem danh sách commit
git brandch  dùng để phân nhánh  
git commit  ghi lại commit đưa lên repo
git merge gộp công việc của các nhánh lại theo thay đổi commit
git rebase gộp công việc của các nhánh lại theo sự bắt đầu của nhánh
git revert quay lại các commit trước đây nhưng k hủy bỏ commit mới
git chery-pick chỉnh sửa thông báo commit